specVersion: "v1beta"
serviceId: "didactinder"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "didactinder-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
